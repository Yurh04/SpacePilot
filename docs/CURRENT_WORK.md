# 当前工作与交接

最后更新：2026-08-24

## 仓库检查点

- 上次文档检查时的分支为 `main`，跟踪 `origin/main`。
- 最近的集成检查点为 `b3113c4`：合并队友的 `feat/double-click` 分支，同时保留应用关联分组、Finder 定位和 AI 管理功能。
- 当前预览版本：0.1.2。
- 当前工作树在 `b3113c4` 基础上包含尚未提交的项目文档，以及本轮豆包关联规则、分析器优先级、应用列表刷新稳定性和测试修改。开始工作前仍必须运行 `git status --short --branch` 检查实时状态。

## 本轮完成

- 内置应用关联知识库升级到 `1.4.0`，新增豆包主应用 `com.bot.pc.doubao` 与签名 Team `96L78H6LMH` 的精确规则。
- 识别 `Library/Application Support/Doubao` 为敏感专属数据，识别 `Library/Caches/Doubao` 为可重建专属缓存；不关联容易误伤的 `~/Doubao`。
- 当知识规则与标准扫描命中同一路径时，只有更高置信度的知识规则可以替换标准结果，不会降级已有的高置信度结构化身份结果。
- 相似 Bundle ID 和错误签名 Team 均不会命中豆包规则。
- 应用详情刷新时保留当前投影，待新投影完成后再一次性替换，避免左侧应用列表被销毁重建并跳回顶部；首次启动的加载状态不变。
- 修复窄窗口布局：移除整窗内容的 `1_000 pt` 强制最小宽度，主导航改为 `160...220 pt` 自适应，应用列表和储存分类栏保留有界宽度但允许收缩，详情工作区不再把父分栏撑出窗口；应用统计卡在窄详情区自动纵向排列。
- 2026-08-24 验证：806 pt 整窗、应用程序分栏、储存空间分栏和侧边栏真实行坐标共 5 项布局测试通过；完整 `swift test` 362 项通过、0 项失败；`./script/build_and_run.sh --verify` 与 `./script/test_release.sh` 的构建、临时签名、启动和应用包检查通过。

## 建议的下一项任务

继续补充下一个经过验证的应用族，仍然一次只处理一个：

1. 收集预期 Bundle ID、嵌入组件、容器、Group Container、启动项、HTTPStorage、偏好设置、日志和安全的命名根目录，但不读取文件正文。
2. 添加最小且明确的知识库规则。
3. 添加测试样例，覆盖正确匹配、共享所有权和相似名称误报。
4. 验证总大小去重和默认清理选择策略。
5. 先运行关联功能测试，再运行 `swift test`。

## 外部阻塞项

公开分发仍需要项目所有者的 Apple Developer 凭据，用于 Developer ID 签名和公证。不能把只有 ad-hoc 签名的 ZIP 描述为适合普通用户公开安装的版本。

## 交接模板

暂停未完成工作时，用实际内容替换以下模板：

```text
目标：
分支和基础提交：
修改文件：
已经完成的行为：
运行的测试和结果：
已知失败场景或阻塞项：
下一步准确操作：
```

本文档应保持简短和最新。已经完成的详细记录写入 Git 提交；长期架构原因写入 `docs/DECISIONS.md`。
