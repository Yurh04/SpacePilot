# 项目开发状态

最后更新：2026-08-28
当前预览版本：0.1.2
支持平台：Apple Silicon、macOS 15+

## 当前阶段

SpacePilot 已经是可以实际运行和试用的本地预览版，不再只是早期原型。核心扫描、持久化索引、增量刷新、应用管理、AI 管理、选择性清理、中英文界面和主要交互流程均已完成。当前重点是继续提高识别完整度、性能与安全性，优化整体 UI，并准备经过正式签名的外部测试版本。

## 已实现

- 原生 SwiftUI macOS 应用，中英文跟随 macOS 系统语言。
- 概览、储存空间、应用程序、开发与 AI、清理历史五个主要模块。
- 概览已升级为操作仪表盘：显示磁盘健康状态、可用空间、安全清理建议和已分析空间，并提供清理、最大项目、应用占用、重新扫描和清理历史快捷入口；图表退居空间详情区域。
- 快速应用清单，以及选中应用后按需执行的深度关联分析。
- 应用详情重新分析期间保留现有列表投影，完成后原位更新，不会因列表重建而跳回顶部。
- 窄窗口不再用 `1_000 pt` 的根内容宽度向两侧溢出；主导航、应用列表、储存分类和详情工作区会在有界范围内协同收缩，应用统计卡在空间不足时改为纵向排列。
- 储存空间的文件表格显示 macOS 系统文件图标；文件夹使用 Finder 文件夹图标，能够解析应用归属的容器会显示对应应用图标。
- 版本化应用关联知识库、Bundle/容器/Team 证据、Spotlight 候选发现、置信度和所有权标记。
- 应用关联文件分组、逐项选择、重置、完整卸载预览、文件图标和双击在 Finder 中显示。
- 豆包应用族使用精确 Bundle ID 与签名 Team 规则识别专属支持数据和缓存，相似标识不会命中。
- SQLite 元数据快照和索引、目录大小缓存、启动恢复、FSEvents 失效处理和按范围后台刷新。
- ChatGPT 与 Codex 产品族合并；Claude 深度分析；Ollama 和 OpenCode 基础空间统计。
- 以本地/远程 Agent 为中心展示应用、CLI、全局和 Agent 专属 Skill、Plugin、Plugin 提供的 Skill、冲突检测和安装证据。
- 仅扫描用户明确批准的项目目录，并按项目和所有者分组其中的 AI 配置、Skill 与 Plugin；越界、重复或失效目录会被拒绝或明确报告。
- 对固定定义且具有可信安装证据的 AI 工具检查版本，并仅通过预定义包管理器命令执行用户确认的更新；不支持或证据冲突的项目保持只读。
- 识别 Xcode/Simulator、Homebrew、npm、Gradle 和 pip 的常见开发储存位置。
- 选择性清理、路径保护、敏感项确认、文件身份复核、移到废纸篓、结果验证和历史记录。
- 限制快照保留项目数量，并把投影工作移出主线程，解决此前超大快照导致的内存问题。

## 部分完成或仍需扩大覆盖

- 应用关联规则有意保持保守，还需为更多应用族和共享容器补充经过验证的规则。
- 完整磁盘分析依赖 macOS 权限；不可访问路径必须继续明确显示为覆盖范围不足。
- 储存分类代表 SpacePilot 已分析的本地数据，不能等同于 macOS 显示的全部“系统数据”。
- 不同 AI 产品的支持深度不同：ChatGPT/Codex 和 Claude 比 Ollama、OpenCode 及新发现工具更深入。
- 本地应用包和 ZIP 可以生成，但正式公开分发仍需配置 Developer ID 签名和 Apple 公证。

## 最近一次代码验证

2026-08-28 对合入 `origin/feat/double-click` 后的 `main` 验证结果：

- `swift test`：481 项测试通过，0 项失败。
- `./script/test_release.sh`：481 项测试、Release 构建、临时应用启动、代码签名结构检查和 ZIP 生成通过。
- Gatekeeper 拒绝仍属预期，因为应用使用的是 ad-hoc 开发签名，而不是经过公证的 Developer ID 签名。

2026-08-26 对基于 `865cbbb` 的概览仪表盘工作树验证结果：

- `./script/test_release.sh`：366 项测试、Release 构建、临时应用启动、代码签名结构检查和 ZIP 生成通过。
- Debug 应用包构建并实际启动；用当前本地快照检查概览的状态、快捷操作、清理建议和最近清理摘要。
- 窄窗口验证：实际把窗口缩到 806 pt，概览指标保持完整，快捷操作自动从四列变为两列，清理建议和主侧边栏没有横向裁切。
- 类别图横轴使用 GB/TB 可读单位，不再显示科学计数法。

## 最近一次发布验证

2026-08-26 对基于 `865cbbb` 的概览仪表盘工作树验证结果：

- `./script/test_release.sh`：366 项测试通过，Release 构建、临时应用启动、代码签名结构检查和 ZIP 生成通过。
- Gatekeeper 拒绝仍属预期，因为应用使用的是 ad-hoc 开发签名，而不是经过公证的 Developer ID 签名。

以上结果都是带日期的检查点，不代表后续提交自动通过。发布新的状态结论前必须重新运行对应验证命令。

## 下一步优先级

1. 以测试和样例为基础扩展高质量应用关联规则，避免宽泛名称匹配造成误报。
2. 在多台有代表性的 Mac 上测量冷启动、应用详情耗时、内存峰值、索引大小和增量刷新行为。
3. 改进权限不完整和索引过期时的界面解释。
4. 完成 Developer ID 签名、公证、发布说明和外部测试版交付流程。
5. 继续检查小窗口布局和所有主要界面的无障碍支持。

## 事实来源

- 当前面向用户的能力：`README.md` 和本文档。
- 分功能完成度：`docs/FEATURE_MATRIX.md`。
- 实际已完成代码：Git 历史和通过的测试。
- 当前工作交接：`docs/CURRENT_WORK.md`。
- 仅供了解历史设计：`docs/superpowers/specs/` 和 `docs/superpowers/plans/`。
